package RecapJavaWithAhmet;

public class StaticKeyWord {

    // is a keyword to make the methods.. etc belongs to the class/
    //*** the advantage of having static keywords is that you don't need tp create an object to access the variables or methods
    // Disvantage of having static keyword is that you cannot oeverride the methods once you call it

    // Static block: is a block that can be called once (1 time it works. It always executed the first

    // Static variable it makes the variables belongd to teh class
    // It is changable for each object
    // It can be accessed by other classes without creating an object

}
